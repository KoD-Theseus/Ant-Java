/*package Creatures.Ants.InitAnt;

public class Init {
    public static void main(String[] args) {
        // Initialize species data
        EfficiencyTree efficiencyTree = new EfficiencyTree();
        efficiencyTree.addSpecies("FormicaRufa", AntData.initializeAntData().get("FormicaRufa"));

        // Query an efficiency
        Float scoutEfficiency = efficiencyTree.getEfficiency("FormicaRufa", "Forager", "ScoutArea");
        if (scoutEfficiency != null) {
            System.out.println("Efficiency of FormicaRufa -> Forager -> ScoutArea: " + scoutEfficiency);
        } else {
            System.out.println("Efficiency data not found!");
        }
    }
}
*/